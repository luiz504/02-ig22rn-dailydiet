import { colors } from './colors'
import { fontFamily } from './fontFamily'
import { fontSize } from './fontSize'
import { lineHeight } from './lineHeight'

export const theme = { colors, fontFamily, fontSize, lineHeight }
